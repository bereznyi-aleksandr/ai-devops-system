{
  "schema_version": 1,
  "trace_id": "kz2c_real_ssot_duplicate_cleanup",
  "created_by": "chatgpt_external_contour",
  "target_executor": "gpt_codex",
  "task_type": "code_patch",
  "title": "KZ-2 real SSOT cleanup",
  "objective": "Run script:\n\"\"\"\nNL = '\\n'\nqueue_path = Path('governance/roadmap/ACTIVE_QUEUE.json')\nif queue_path.exists():\n    queue = json.loads(queue_path.read_text(encoding='utf-8'))\nelse:\n    queue = {'tasks': []}\nfor task in queue.get('tasks', []):\n    if task.get('id') == 'KZ-2':\n        task['status'] = 'IN_PROGRESS'\n        task['audit_reopened_sha'] = '4d93026f'\nqueue['updated_at'] = '2026-06-04T15:20:00Z'\nqueue_path.write_text(json.dumps(queue, ensure_ascii=False, indent=2) + NL, encoding='utf-8')\nchanged_files.append(str(queue_path))\narchive_root = Path('governance/archive/real_ssot_duplicates_2026-06-04')\narchive_root.mkdir(parents=True, exist_ok=True)\nssot_names = ['contours_registry.json','object_passports.json','providers_registry.json','managed_channels_registry.json','channels_registry.json','objects_registry.json','release_proof_manifest.json','ssot_canonical_paths.json']\ncanonical_priority = [Path('governance/state'), Path('governance/release'), Path('governance/contours'), Path('governance/objects'), Path('governance/providers'), Path('governance/channels')]\nfound = {}\nfor name in ssot_names:\n    paths = []\n    for p in Path('governance').rglob(name):\n        s = str(p)\n        if '/archive/' in s or '/codex/' in s:\n            continue\n        if p.is_file():\n            paths.append(p)\n    found[name] = [str(p) for p in paths]\nkept = []\nmoved = []\nfor name, paths_s in found.items():\n    paths = [Path(x) for x in paths_s]\n    if len(paths) <= 1:\n        if paths:\n            kept.append(str(paths[0]))\n        continue\n    keep = None\n    for root in canonical_priority:\n        candidate = root / name\n        if candidate in paths:\n            keep = candidate\n            break\n    if keep is None:\n        keep = sorted(paths)[0]\n    kept.append(str(keep))\n    for p in paths:\n        if p == keep:\n            continue\n        target = archive_root / p.name\n        n = 1\n        while target.exists():\n            target = archive_root / (p.stem + '_' + str(n) + p.suffix)\n            n = n + 1\n        p.rename(target)\n        changed_files.append(str(p))\n        changed_files.append(str(target))\n        moved.append({'from': str(p), 'to': str(target), 'kept': str(keep), 'name': name})\nremaining = {}\nfor name in ssot_names:\n    active = []\n    for p in Path('governance').rglob(name):\n        s = str(p)\n        if '/archive/' in s or '/codex/' in s:\n            continue\n        if p.is_file():\n            active.append(str(p))\n    if len(active) > 1:\n        remaining[name] = active\nall_valid = len(remaining) == 0\nstatus_path = Path('governance/state/kz2_ssot_status.json')\nstatus_path.parent.mkdir(parents=True, exist_ok=True)\nstatus_path.write_text(json.dumps({'status':'PASS' if all_valid else 'BLOCKED','all_valid':all_valid,'target_names':ssot_names,'found_before':found,'kept':kept,'moved':moved,'remaining_duplicates':remaining,'workflow_files_touched':False}, ensure_ascii=False, indent=2) + NL, encoding='utf-8')\nchanged_files.append(str(status_path))\nstate = Path('governance/state/KZ2C_REAL_SSOT_DUPLICATE_CLEANUP.json')\nstate.write_text(json.dumps({'id':'KZ-2','trace_id':'kz2c_real_ssot_duplicate_cleanup','status':'completed' if all_valid else 'blocked','release_pass':False,'all_valid':all_valid,'moved_count':len(moved),'remaining_duplicate_group_count':len(remaining),'kz2_ssot_status':str(status_path),'workflow_files_touched':False,'next':'KZ-2D record real SSOT cleanup result' if all_valid else 'KZ-2 repair remaining duplicates'}, ensure_ascii=False, indent=2) + NL, encoding='utf-8')\nchanged_files.append(str(state))\nreport = Path('governance/reports/KZ2C_REAL_SSOT_DUPLICATE_CLEANUP.md')\nreport.write_text('# KZ-2C | Real SSOT duplicate cleanup' + NL + 'Status: ' + ('completed' if all_valid else 'blocked') + NL + 'Date: 2026-06-04' + NL + NL + 'Moved real active SSOT duplicate files into ' + str(archive_root) + '.' + NL + 'Moved count: ' + str(len(moved)) + NL + 'Remaining duplicate groups: ' + str(len(remaining)) + NL + NL + 'Status file: governance/state/kz2_ssot_status.json' + NL + 'Workflow lock respected. No .github/workflows files touched.' + NL + NL + 'Next: ' + ('KZ-2D record real SSOT cleanup result.' if all_valid else 'KZ-2 repair remaining duplicates.') + NL, encoding='utf-8')\nchanged_files.append(str(report))\n\"\"\"No issue comments.",
  "constraints": [
    "no issue comments",
    "no schedule triggers",
    "minimal patch",
    "write result files only"
  ],
  "expected_outputs": {
    "result_md": "governance/codex/results/kz2c_real_ssot_duplicate_cleanup.md",
    "result_json": "governance/codex/results/kz2c_real_ssot_duplicate_cleanup.json"
  },
  "created_at": "2026-06-04T15:11:43.248Z"
}
