{
  "schema_version": 1,
  "trace_id": "bem866_isolate_queue_runner",
  "created_by": "chatgpt_external_contour",
  "target_executor": "gpt_codex",
  "task_type": "code_patch",
  "title": "BEM-866 isolate queue runner",
  "objective": "Run script:\n\"\"\"\n# 1. Remove all workflow dispatch queue processing from codex-runner.yml.\ncodex = Path('.github/workflows/codex-runner.yml')\ntext = codex.read_text(encoding='utf-8')\nremove_names = [\n    'Process workflow dispatch queue before commit',\n    'Process workflow dispatch queue via github-script',\n    'Process workflow dispatch queue minimal',\n    'Process workflow dispatch queue',\n    'BEM-866 queue step heartbeat'\n]\nlines = text.splitlines()\nout = []\ni = 0\nwhile i < len(lines):\n    line = lines[i]\n    if any(('- name: ' + name) in line for name in remove_names):\n        indent = len(line) - len(line.lstrip(' '))\n        i += 1\n        while i < len(lines):\n            nxt = lines[i]\n            nindent = len(nxt) - len(nxt.lstrip(' '))\n            if nxt.lstrip().startswith('- name:') and nindent == indent:\n                break\n            i += 1\n        continue\n    if 'BEM-865 inline workflow dispatch queue marker' in line or 'Queue processing is handled after task execution' in line:\n        i += 1\n        continue\n    out.append(line)\n    i += 1\ncodex.write_text('\\n'.join(out).rstrip() + '\\n', encoding='utf-8')\n\n# 2. Write dedicated isolated queue runner workflow.\nwf = Path('.github/workflows/workflow_dispatch_queue_runner.yml')\nwf.parent.mkdir(parents=True, exist_ok=True)\nwf.write_text('''name: workflow_dispatch_queue_runner\n\non:\n  workflow_dispatch:\n\npermissions:\n  contents: write\n  actions: write\n\njobs:\n  dispatch_queue:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout\n        uses: actions/checkout@v4\n        with:\n          token: ${{ secrets.AI_SYSTEM_GITHUB_PAT }}\n\n      - name: Dispatch queued workflows\n        uses: actions/github-script@v7\n        with:\n          github-token: ${{ secrets.AI_SYSTEM_GITHUB_PAT }}\n          script: |\n            const fs = require('fs');\n            const path = require('path');\n            const queueDir = 'governance/workflow_dispatch_queue';\n            const processedDir = 'governance/workflow_dispatch_processed';\n            const resultsDir = 'governance/workflow_dispatch_results';\n            fs.mkdirSync(queueDir, { recursive: true });\n            fs.mkdirSync(processedDir, { recursive: true });\n            fs.mkdirSync(resultsDir, { recursive: true });\n            const files = fs.readdirSync(queueDir).filter((f) => f.endsWith('.json')).sort();\n            for (const file of files) {\n              const full = path.join(queueDir, file);\n              const item = JSON.parse(fs.readFileSync(full, 'utf8'));\n              const workflow_id = item.workflow;\n              const ref = item.ref || 'main';\n              const inputs = item.inputs || {};\n              let status = 'DISPATCHED';\n              let error = null;\n              try {\n                await github.rest.actions.createWorkflowDispatch({ owner: context.repo.owner, repo: context.repo.repo, workflow_id, ref, inputs });\n              } catch (e) {\n                status = 'DISPATCH_FAILED';\n                error = String(e).slice(0, 1000);\n              }\n              fs.writeFileSync(path.join(resultsDir, file.replace(/\\\\.json$/, '_result.json')), JSON.stringify({ status, workflow_id, ref, inputs, error, dispatched_at: new Date().toISOString() }, null, 2));\n              fs.renameSync(full, path.join(processedDir, file));\n            }\n\n      - name: Commit dispatch state\n        run: |\n          git config user.name \"ai-devops-system\"\n          git config user.email \"actions@users.noreply.github.com\"\n          git add governance/workflow_dispatch_queue governance/workflow_dispatch_processed governance/workflow_dispatch_results || true\n          if ! git diff --cached --quiet; then\n            git commit -m \"BEM-866 process workflow dispatch queue\"\n            git push\n          fi\n''', encoding='utf-8')\n\nmarker = Path('governance/runtime/curator_dispatch/BEM866_QUEUE_RUNNER_ISOLATED.md')\nmarker.parent.mkdir(parents=True, exist_ok=True)\nmarker.write_text('# BEM-866 queue runner isolated\\nStatus: ISOLATED\\n- codex-runner.yml cleaned\\n- workflow_dispatch_queue_runner.yml is standalone workflow_dispatch only\\n', encoding='utf-8')\nchanged_files.append(str(codex))\nchanged_files.append(str(wf))\nchanged_files.append(str(marker))\nops_applied.append('isolated workflow dispatch queue runner and cleaned codex-runner')\n\"\"\"\nNo issue comments.",
  "constraints": [
    "no issue comments",
    "no schedule triggers",
    "minimal patch",
    "write result files only"
  ],
  "expected_outputs": {
    "result_md": "governance/codex/results/bem866_isolate_queue_runner.md",
    "result_json": "governance/codex/results/bem866_isolate_queue_runner.json"
  },
  "created_at": "2026-05-26T04:37:35.306Z"
}
