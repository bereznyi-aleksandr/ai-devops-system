{
  "schema_version": 1,
  "trace_id": "bem584_fix_decision_renderer_no_newline_literal",
  "created_by": "chatgpt_external_contour",
  "target_executor": "gpt_codex",
  "task_type": "code_patch",
  "title": "BEM-584 fix decision renderer",
  "objective": "Run script:\n\"\"\"\nsep='\\n'\nscripts=Path('scripts'); scripts.mkdir(parents=True,exist_ok=True)\nwfs=Path('.github/workflows'); wfs.mkdir(parents=True,exist_ok=True)\nqueue=Path('governance/operator_decision_queue'); queue.mkdir(parents=True,exist_ok=True)\nreports=Path('governance/reports'); reports.mkdir(parents=True,exist_ok=True)\nproofs=Path('governance/codex/proofs'); proofs.mkdir(parents=True,exist_ok=True)\ntransport=Path('governance/transport/results.jsonl'); transport.parent.mkdir(parents=True,exist_ok=True)\ncontour=Path('governance/state/contour_status.json'); contour.parent.mkdir(parents=True,exist_ok=True)\n\npick=Path('scripts/operator_decision_pick.py')\nsource=[\n'#!/usr/bin/env python3',\n'import json',\n'from pathlib import Path',\n'SEP = bytes.fromhex(\"0a\").decode(\"ascii\")',\n'QUEUE_DIR = Path(\"governance/operator_decision_queue\")',\n'STATE_PATH = Path(\"governance/state/operator_decision_dispatcher_state.json\")',\n'TMP_DIR = Path(\"governance/tmp\")',\n'PICK_PATH = TMP_DIR / \"operator_decision_pick.json\"',\n'MSG_PATH = TMP_DIR / \"operator_decision_message.txt\"',\n'def load_json(path, default):',\n'    if path.exists():',\n'        try:',\n'            return json.loads(path.read_text(encoding=\"utf-8\"))',\n'        except Exception:',\n'            return default',\n'    return default',\n'def pct(done, total):',\n'    try:',\n'        done = int(done); total = int(total)',\n'        return 0 if total <= 0 else round(done * 100 / total)',\n'    except Exception:',\n'        return 0',\n'def short(text, limit):',\n'    value = \" \".join(str(text or \"\").split())',\n'    return value if len(value) <= limit else value[:max(0, limit-1)].rstrip() + \"…\"',\n'def pad(text, width):',\n'    value = short(text, width)',\n'    return value + \" \" * max(0, width - len(value))',\n'def validate(pkg):',\n'    if not pkg.get(\"decision_id\") or not pkg.get(\"question\"):',\n'        return False, \"missing_decision_id_or_question\"',\n'    opts = pkg.get(\"options\") or []',\n'    if len(opts) < 2:',\n'        return False, \"need_at_least_two_options\"',\n'    for opt in opts:',\n'        if not opt.get(\"id\") or not opt.get(\"title\") or not opt.get(\"difference\") or not opt.get(\"rationale\"):',\n'            return False, \"option_missing_required_fields\"',\n'    return True, None',\n'def make_table(options):',\n'    rows = []',\n'    rows.append(\"┌────┬──────────────┬──────────────────────┐\")',\n'    rows.append(\"│ №  │ Вариант      │ Отличие / обоснование │\")',\n'    rows.append(\"├────┼──────────────┼──────────────────────┤\")',\n'    for opt in options[:4]:',\n'        oid = pad(opt.get(\"id\", \"?\"), 2)',\n'        title = pad(opt.get(\"title\", \"\"), 12)',\n'        combo = pad(short(opt.get(\"difference\", \"\"), 10) + \"; \" + short(opt.get(\"rationale\", \"\"), 10), 20)',\n'        rows.append(\"│ \" + oid + \" │ \" + title + \" │ \" + combo + \" │\")',\n'    rows.append(\"└────┴──────────────┴──────────────────────┘\")',\n'    return SEP.join(rows)',\n'def make_details(options):',\n'    rows = []',\n'    for opt in options[:4]:',\n'        oid = str(opt.get(\"id\", \"?\"))',\n'        rows.append(oid + \") \" + str(opt.get(\"title\", \"\")))',\n'        rows.append(\"   Отличие: \" + short(opt.get(\"difference\", \"\"), 160))',\n'        rows.append(\"   Обоснование: \" + short(opt.get(\"rationale\", \"\"), 180))',\n'        if opt.get(\"impact\"):',\n'            rows.append(\"   Последствие: \" + short(opt.get(\"impact\", \"\"), 160))',\n'        rows.append(\"\")',\n'    return SEP.join(rows).strip()',\n'def build_message(pkg):',\n'    stage = pkg.get(\"stage\") or {\"done\":1,\"total\":1}',\n'    roadmap = pkg.get(\"roadmap\") or {\"done\":1,\"total\":1}',\n'    sd = stage.get(\"done\",1); st = stage.get(\"total\",1)',\n'    rd = roadmap.get(\"done\",1); rt = roadmap.get(\"total\",1)',\n'    opts = pkg.get(\"options\") or []',\n'    lines = [str(pkg.get(\"bem\",\"BEM-MAILBOX\")) + \" | \" + str(pkg.get(\"title\",\"OPERATOR DECISION REQUIRED\")) + \" | workflow_runtime\", \"\", \"Этап: \" + str(sd) + \"/\" + str(st) + \" (\" + str(pct(sd,st)) + \"%)\", \"Дорожная карта: \" + str(rd) + \"/\" + str(rt) + \" (\" + str(pct(rd,rt)) + \"%)\", \"\", \"Чек-лист:\"]',\n'    for item in (pkg.get(\"checklist\") or [\"Claude/GPT обсуждение завершено\", \"Decision package подготовлен\", \"Требуется решение оператора\"])[:6]:',\n'        item = str(item).strip()',\n'        lines.append(item if item.startswith((\"✅\",\"⚠️\",\"❌\")) else \"✅ \" + item)',\n'    lines += [\"\", \"Вопрос:\", str(pkg.get(\"question\",\"\")).strip(), \"\", \"Таблица сравнения:\", make_table(opts), \"\", \"Детали по вариантам:\", make_details(opts)]',\n'    rec = pkg.get(\"recommendation\") or {}',\n'    if rec:',\n'        lines += [\"\", \"Рекомендация аудиторов:\", \"Вариант \" + str(rec.get(\"option_id\",\"?\")) + \": \" + str(rec.get(\"rationale\",\"\"))]',\n'    lines += [\"\", \"Как ответить:\", str(pkg.get(\"reply_format\",\"Напиши 1, 2, 3 или свой вариант текстом.\")), \"\", \"Что будет после решения:\", \"Пакет решения передаётся куратору, затем во внутренний контур.\"]',\n'    return SEP.join(lines).strip() + SEP',\n'state = load_json(STATE_PATH, {})',\n'processed = set(state.get(\"processed_items\", []))',\n'QUEUE_DIR.mkdir(parents=True, exist_ok=True)',\n'candidates = sorted([p for p in QUEUE_DIR.glob(\"*.json\") if p.is_file()], key=lambda p: str(p))',\n'picked_path = None; pkg = None',\n'for path in candidates:',\n'    if str(path) not in processed:',\n'        picked_path = path; pkg = load_json(path, {}); break',\n'TMP_DIR.mkdir(parents=True, exist_ok=True)',\n'if picked_path is None:',\n'    result = {\"picked\": False, \"send\": False, \"queue_file\": None, \"reason\": \"no_new_decision_package\"}',\n'    MSG_PATH.write_text(\"\", encoding=\"utf-8\")',\n'else:',\n'    ok, reason = validate(pkg)',\n'    if ok:',\n'        message = build_message(pkg)',\n'        result = {\"picked\": True, \"send\": True, \"queue_file\": str(picked_path), \"decision_id\": pkg.get(\"decision_id\"), \"reason\": \"valid_decision_package\", \"message\": message}',\n'        MSG_PATH.write_text(message, encoding=\"utf-8\")',\n'    else:',\n'        result = {\"picked\": True, \"send\": False, \"queue_file\": str(picked_path), \"reason\": \"invalid_decision_package:\" + str(reason)}',\n'        MSG_PATH.write_text(\"\", encoding=\"utf-8\")',\n'PICK_PATH.write_text(json.dumps(result, ensure_ascii=False, indent=2) + SEP, encoding=\"utf-8\")',\n'print(json.dumps({\"picked\": result.get(\"picked\"), \"send\": result.get(\"send\"), \"reason\": result.get(\"reason\"), \"queue_file\": result.get(\"queue_file\")}, ensure_ascii=False))',\n]\npick.write_text(sep.join(source)+sep)\n\nrec=Path('scripts/operator_decision_record_dispatch.py')\nrec_source=[\n'#!/usr/bin/env python3',\n'import json',\n'from pathlib import Path',\n'SEP = bytes.fromhex(\"0a\").decode(\"ascii\")',\n'STATE_PATH = Path(\"governance/state/operator_decision_dispatcher_state.json\")',\n'PICK_PATH = Path(\"governance/tmp/operator_decision_pick.json\")',\n'STATUS_PATH = Path(\"governance/tmp/operator_decision_telegram_status.txt\")',\n'TRANSPORT_PATH = Path(\"governance/transport/results.jsonl\")',\n'REPORT_PATH = Path(\"governance/reports/operator_decision_dispatch_result.md\")',\n'def load_json(path, default):',\n'    if path.exists():',\n'        try:',\n'            return json.loads(path.read_text(encoding=\"utf-8\"))',\n'        except Exception:',\n'            return default',\n'    return default',\n'def append_jsonl(path, record):',\n'    path.parent.mkdir(parents=True, exist_ok=True)',\n'    with path.open(\"a\", encoding=\"utf-8\") as handle:',\n'        handle.write(json.dumps(record, ensure_ascii=False) + SEP)',\n'state = load_json(STATE_PATH, {})',\n'pick = load_json(PICK_PATH, {\"picked\": False, \"send\": False})',\n'status = STATUS_PATH.read_text(encoding=\"utf-8\", errors=\"ignore\").strip() if STATUS_PATH.exists() else \"not_required\"',\n'if pick.get(\"picked\"):',\n'    processed = set(state.get(\"processed_items\", []))',\n'    processed.add(pick.get(\"queue_file\"))',\n'    state[\"processed_items\"] = sorted(processed)',\n'record = {\"record_type\":\"operator_decision_dispatch\",\"cycle_id\":\"bem584-fix-decision-renderer-no-newline-literal\",\"queue_file\":pick.get(\"queue_file\"),\"decision_id\":pick.get(\"decision_id\"),\"send\":pick.get(\"send\"),\"reason\":pick.get(\"reason\"),\"telegram_status\":status if pick.get(\"send\") else \"not_sent\",\"created_at\":\"workflow_runtime\",\"blocker\":None if (not pick.get(\"send\") or status == \"sent\") else {\"code\":\"OPERATOR_DECISION_TELEGRAM_NOT_SENT\",\"status\":status}}',\n'append_jsonl(TRANSPORT_PATH, record)',\n'state[\"last_run\"] = record',\n'STATE_PATH.parent.mkdir(parents=True, exist_ok=True)',\n'STATE_PATH.write_text(json.dumps(state, indent=2, ensure_ascii=False) + SEP, encoding=\"utf-8\")',\n'REPORT_PATH.parent.mkdir(parents=True, exist_ok=True)',\n'REPORT_PATH.write_text(\"# Operator Decision Dispatch Result\" + SEP + SEP + json.dumps(record, ensure_ascii=False, indent=2) + SEP, encoding=\"utf-8\")',\n'print(json.dumps(record, ensure_ascii=False))',\n]\nrec.write_text(sep.join(rec_source)+sep)\n\n# rewrite valid package\npkg={\n'schema_version':'operator_decision_queue.v1','decision_id':'bem584_decision_format_live_test','bem':'BEM-584','title':'OPERATOR DECISION REQUIRED','stage':{'done':1,'total':1},'roadmap':{'done':1,'total':1},\n'checklist':['✅ Routine mailbox Telegram disabled','✅ Structured decision package prepared','✅ Readable table renderer fixed','⚠️ Operator must approve decision-message format'],\n'question':'Какой формат Telegram-сообщения использовать для решений оператора?',\n'options':[{'id':'1','title':'Короткая таблица','difference':'Компактное сравнение вариантов','rationale':'Видно вопрос и сравнение без длинной каши','impact':'Используем формат для operator decisions'},{'id':'2','title':'Вертикальный список','difference':'Каждый вариант отдельным блоком','rationale':'Проще, но хуже сравнивать варианты','impact':'Оставляем менее удобный формат'},{'id':'3','title':'Доработать','difference':'Новый макет до внедрения','rationale':'Если таблица всё ещё неудобна','impact':'Возврат Claude/GPT на доработку'}],\n'recommendation':{'option_id':'1','rationale':'Рекомендуем короткую таблицу: она сохраняет канон, вопрос, варианты, отличия и обоснование в одном сообщении.'},\n'reply_format':'Напиши 1, 2, 3 или свой вариант текстом.','handoff_to':'curator'}\nqfile=queue/'bem584_decision_format_live_test.json'\nqfile.write_text(json.dumps(pkg,ensure_ascii=False,indent=2)+sep)\n\nreport=reports/'bem584_fix_decision_renderer_no_newline_literal.md'\nreport.write_text('# BEM-584 | Fix Decision Renderer No Newline Literal | PASS'+sep+sep+'Дата: 2026-05-17 | 22:09 (UTC+3)'+sep+sep+'Renderer and recorder now use bytes.fromhex(\"0a\") internally; no literal newline string. Valid live-test decision package recreated.'+sep+sep+'Blocker: null'+sep)\nrecord={'record_type':'bem584_fix_decision_renderer_no_newline_literal','cycle_id':'bem584-fix-decision-renderer-no-newline-literal','source':'gpt','status':'completed','queue_file':str(qfile),'renderer':str(pick),'recorder':str(rec),'blocker':None,'created_at':'2026-05-17 | 22:09 (UTC+3)'}\nwith transport.open('a',encoding='utf-8') as handle: handle.write(json.dumps(record,ensure_ascii=False)+sep)\nstate={}\nif contour.exists():\n    try: state=json.loads(contour.read_text(errors='ignore'))\n    except Exception: state={}\nstate['bem584_fix_decision_renderer_no_newline_literal']={'status':'completed','queue_file':str(qfile),'blocker':None}\nstate['updated_at']='2026-05-17 | 22:09 (UTC+3)'; state['blocker']=None\ncontour.write_text(json.dumps(state,indent=2,ensure_ascii=False)+sep)\nproof=proofs/'bem584_fix_decision_renderer_no_newline_literal.txt'\nproof.write_text('BEM-584 PASS: fixed operator decision renderer/recorder no newline literal and created valid live-test package.\\n')\nchanged_files.extend([str(pick),str(rec),str(qfile),str(report),str(transport),str(contour),str(proof)])\nops_applied.append('Fixed operator decision renderer/recorder without newline literals and recreated valid live-test package')\n\"\"\"\nNo issue comments.",
  "constraints": [
    "no issue comments",
    "no schedule triggers",
    "minimal patch",
    "write result files only"
  ],
  "expected_outputs": {
    "result_md": "governance/codex/results/bem584_fix_decision_renderer_no_newline_literal.md",
    "result_json": "governance/codex/results/bem584_fix_decision_renderer_no_newline_literal.json"
  },
  "created_at": "2026-05-17T19:22:10.385Z"
}
