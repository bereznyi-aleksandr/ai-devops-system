{
  "version": "1.4",
  "updated_at": "2026-06-27",
  "note": "Paid OpenAI API (OPENAI_API_KEY) is PERMANENTLY PROHIBITED by operator. gpt_codex_cloud removed as fallback. Claude Code via CLAUDE_CODE_OAUTH_TOKEN is the only authorized executor.",
  "providers": {
    "claude_code": {
      "enabled": true,
      "priority": 1,
      "workflow_id": "claude.yml",
      "billing_class": "subscription_included",
      "description": "Claude Code Action via CLAUDE_CODE_OAUTH_TOKEN — primary and only executor",
      "headless": true
    },
    "gpt_codex_cloud": {
      "enabled": false,
      "deprecated": true,
      "deprecation_reason": "Used OPENAI_API_KEY (paid OpenAI API). Permanently prohibited by operator. Disabled 2026-06-27.",
      "workflow_id": "gpt-codex-cloud.yml"
    },
    "gpt_codex": {
      "enabled": false,
      "deprecated": true,
      "deprecation_reason": "self-hosted runner required — never provisioned",
      "workflow_id": "codex-local-assembled.yml"
    }
  },
  "paid_api_policy": {
    "openai_api_key": "PERMANENTLY_PROHIBITED",
    "openai_model_var": "NOT_NEEDED",
    "automatic_paid_fallback": "PERMANENTLY_PROHIBITED",
    "violation_action": "BLOCKED_COST_POLICY — do not proceed, create receipt, notify operator"
  }
}
