name: Autonomous Development Loop

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

concurrency:
  group: autonomous-dev-loop
  cancel-in-progress: true

permissions:
  contents: write
  actions: write

jobs:
  loop:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.AI_SYSTEM_GITHUB_PAT }}

      # Поставщик Gemini отменён решением оператора 2026-08-24 и из системы удалён.
      # Ключ Gemini больше не передаётся в этот шаг.
      - name: Read ACTIVE_QUEUE and dispatch GPT role
        env:
          GH_PAT: ${{ secrets.AI_SYSTEM_GITHUB_PAT }}
          REPO: ${{ github.repository }}
        run: |
          python3 - <<'PY'
          import json, os, urllib.request, urllib.error
          from datetime import datetime, timezone
          from pathlib import Path

          gh_pat = os.environ['GH_PAT']
          repo   = os.environ['REPO']
          api    = 'https://api.github.com'
          now    = datetime.now(timezone.utc).strftime('%Y%m%dT%H%M%SZ')

          def dispatch(workflow, inputs):
              body = json.dumps({'ref': 'main', 'inputs': inputs}).encode()
              req = urllib.request.Request(
                  f'{api}/repos/{repo}/actions/workflows/{workflow}/dispatches',
                  data=body, method='POST',
                  headers={'Authorization': f'Bearer {gh_pat}',
                           'Accept': 'application/vnd.github+json',
                           'Content-Type': 'application/json',
                           'User-Agent': 'autonomous-dev-loop'}
              )
              try:
                  with urllib.request.urlopen(req, timeout=10) as r:
                      print(f'Dispatched {workflow}: HTTP {r.status}')
                      return True
              except urllib.error.HTTPError as e:
                  print(f'Dispatch error {workflow}: {e.code} {e.read()[:200]}')
                  return False

          # Читаем ACTIVE_QUEUE
          queue_path = Path('governance/roadmap/ACTIVE_QUEUE.json')
          if not queue_path.exists():
              print('ACTIVE_QUEUE.json not found — nothing to do')
              raise SystemExit(0)

          queue = json.loads(queue_path.read_text())
          tasks = queue.get('tasks', [])

          # Берём первую IN_PROGRESS или PENDING задачу
          next_task = None
          for t in tasks:
              if t.get('status') in ('IN_PROGRESS', 'PENDING'):
                  next_task = t
                  break

          if not next_task:
              print('ACTIVE_QUEUE empty — nothing to do')
              raise SystemExit(0)

          task_id    = next_task.get('id', 'unknown')
          task_title = next_task.get('title', '')
          task_detail = next_task.get('detail', '')
          touch_wf   = next_task.get('touch_workflows', False)

          print(f'Next task: {task_id} — {task_title}')

          # Если задача требует изменения workflow — это работа для Claude, не GPT
          if touch_wf:
              print(f'Task {task_id} requires workflow changes — locked for GPT. Skipping.')
              raise SystemExit(0)

          # Формируем trace_id
          trace_id = f'auto_{task_id}_{now}'

          # Запускаем GPT analyst для анализа задачи
          task_body = f'TASK_ID: {task_id}\nTITLE: {task_title}\n\nDETAIL:\n{task_detail}'

          # Прежний маршрут вёл в gpt-hosted-roles.yml с поставщиком Gemini.
          # Он закрыт по двум причинам сразу:
          #   1. Gemini отменён решением оператора 2026-08-24 и из системы удалён.
          #   2. Сам gpt-hosted-roles.yml заархивирован ещё в BEM-931: он печатает
          #      "ARCHIVED" и выходит, то есть работу не выполнял в любом случае.
          # Новый маршрут исполнения назначается первой волной дорожной карты BEM-954.
          #
          # Пока маршрута нет, задача остаётся активной в очереди. Молча выйти с
          # успехом здесь нельзя: прогон стал бы зелёным, очередь бы не двигалась,
          # и на следующем часе тот же таск выбрался бы заново — ровно тот дефект,
          # ради устранения которого правка и делалась.
          # Поэтому попытка записывается в сводку прогона и завершается отказом.
          blocker = {
              'status': 'BLOCKED_NO_ROUTE',
              'task_id': task_id,
              'trace_id': trace_id,
              'reason': 'Маршрут исполнения не назначен: прежний вёл в заархивированный gpt-hosted-roles.yml',
              'next_action': 'Назначить маршрут первой волной дорожной карты BEM-954',
              'recorded_at': datetime.now(timezone.utc).isoformat(),
          }
          line = json.dumps(blocker, ensure_ascii=False)
          print(line)

          summary = os.environ.get('GITHUB_STEP_SUMMARY')
          if summary:
              with open(summary, 'a', encoding='utf-8') as fh:
                  fh.write('### BLOCKED_NO_ROUTE\n\n')
                  fh.write(f'Задача `{task_id}` не отправлена: маршрут исполнения не назначен.\n\n')
                  fh.write(f'```json\n{line}\n```\n')

          raise SystemExit(1)
          PY
