name: autonomy-entrypoint
# DISABLED: старый автономный контур отключён 2026-04-29
# Заменён новой архитектурой: АУДИТОР + Claude Code через Issues

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Причина ручного запуска'
        required: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "DISABLED: старый автономный контур отключён."
          echo "Используй новую схему: АУДИТОР создаёт issue с @claude."
