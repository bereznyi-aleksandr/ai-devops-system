name: DEPRECATED - Issue To Claude Dispatch (superseded)

# SUPERSEDED 2026-06-21: this bridge was built to work around a believed
# gap in GPT's tool surface (no direct workflow_dispatch capability).
# That gap already had a correct, deployed solution:
#
#   infrastructure/cloudflare-worker/telegram-webhook.js
#   POST /gpt-dispatch  (header: x-gpt-secret)
#   { "workflow_id": "<any>.yml", "inputs": {...} }
#
# This is the canonical BEM-932 mechanism (Cloudflare Worker, full
# arbitrary-workflow dispatch, already deployed/receipt-verified).
# Routing GPT task submission through GitHub Issues was unnecessary
# indirection. Use the Worker /gpt-dispatch endpoint directly instead.
#
# This file is kept (disabled) only as a historical record, not deleted,
# per the repository's archive-don't-delete convention.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "DEPRECATED - use Cloudflare Worker /gpt-dispatch instead. See infrastructure/cloudflare-worker/telegram-webhook.js"
