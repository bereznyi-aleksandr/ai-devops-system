# DISABLED — E3-CLEANUP Этап 0B — 2026-05-03
# Заменён новой архитектурой: curator-on-operator-message.yml + telegram-outbox-dispatch.yml
name: DISABLED
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "DISABLED"
