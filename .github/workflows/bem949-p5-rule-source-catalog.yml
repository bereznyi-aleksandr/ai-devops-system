name: BEM949 P5 Evidence Check
"on":
  workflow_dispatch:
jobs:
  verify:
    runs-"on": ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify P5 receipt
        run: test -s governance/proofs/BEM949_p5_rule_enforcement_complete_receipt.json
