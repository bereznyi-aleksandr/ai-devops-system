name: DISABLED - legacy stray file

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow sentinel
        run: echo "This legacy file is intentionally inert."
