# ARCHIVED — repair-claude-transport-persistence.yml
# The prior disabled-stub source is retained under governance/archive/disabled-workflows/.
# This workflow is intentionally inert but remains syntactically valid for GitHub Actions.
name: ARCHIVED repair-claude-transport-persistence.yml
on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Archived workflow; execution is intentionally disabled."
        required: false
        default: "ARCHIVED"
        type: string
jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "ARCHIVED"
