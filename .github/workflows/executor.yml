name: Executor

on:
  issue_comment:
    types: [created]
  issues:
    types: [opened, assigned]

jobs:
  executor:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@executor')) ||
      (github.event_name == 'issues' && (contains(github.event.issue.body, '@executor') || contains(github.event.issue.title, '@executor')))

    runs-on: ubuntu-latest

    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run Executor (Claude Code)
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          prompt_file: governance/prompts/executor_prompt.md
          claude_args: |
            --max-turns 15
