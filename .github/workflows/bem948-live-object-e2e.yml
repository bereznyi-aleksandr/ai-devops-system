name: BEM-948 Live Object To Claude E2E

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'governance/state/bem948_e2e_trigger.json'
      - '.github/workflows/bem948-live-object-e2e.yml'

permissions:
  contents: write
  actions: write

jobs:
  live_e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      TRACE_ID: bem948_p0_live_object_e2e_observer_20260620T0510Z
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.AI_SYSTEM_GITHUB_PAT || github.token }}
          fetch-depth: 1

      - name: Queue and route minimal health task
        shell: bash
        run: |
          set -euo pipefail
          python3 governance/runners/object_runner.py \
            --object-id OBJ-GD-001 \
            --role executor \
            --provider claude_code \
            --trace-id "$TRACE_ID" \
            --consume \
            --task "BEM-948 P0 observed object-to-dispatch health test. Create exactly two files: governance/reports/bem948_p0_live_object_e2e_observer_20260620T0510Z.md and governance/proofs/BEM948_p0_live_object_executed_bem948_p0_live_object_e2e_observer_20260620T0510Z.json. The JSON must truthfully state completed only after work is done; include protocol BEM-948, this trace_id, provider_selected claude_code, target_workflow_id claude.yml, dispatch_result executed, blocker null, and sha_type for every SHA. Do not modify workflows."

      - name: Execute only this routed dispatch
        id: executor
        shell: bash
        env:
          AI_SYSTEM_GITHUB_PAT: ${{ secrets.AI_SYSTEM_GITHUB_PAT }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_API_URL: ${{ github.api_url }}
        run: |
          set +e
          python3 governance/runners/dispatch_executor.py --max 1 --trace-id "$TRACE_ID"
          result=$?
          set -e
          echo "result=$result" >> "$GITHUB_OUTPUT"

      - name: Commit P0 handoff evidence
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "BEM-948 Live E2E"
          git config user.email "bem948-live-e2e@ai-devops-system"
          git add governance/state/ governance/proofs/BEM948_dispatch_executor_receipt.json 2>/dev/null || true
          if ! git diff --cached --quiet; then
            git commit -m "BEM-948: dispatch trace-bound live object handoff"
            git push
          fi

      - name: Enforce dispatch handoff result
        if: always()
        shell: bash
        run: |
          test "${{ steps.executor.outputs.result }}" = "0"
