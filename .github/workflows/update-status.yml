# DISABLED — BEM-281
# update-status.yml отключён — сломан YAML синтаксис
# Заменён telegram-send.yml
name: DISABLED
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "DISABLED"
