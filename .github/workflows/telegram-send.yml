# DISABLED — лишний workflow
# GPT пишет в Telegram напрямую через API без workflows
name: DISABLED
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "DISABLED — GPT пишет в Telegram напрямую"
