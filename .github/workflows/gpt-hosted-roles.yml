name: ARCHIVED - GPT Hosted Roles (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived runtime
        run: |
          echo "ARCHIVED: gpt-hosted-roles.yml is disabled."
          echo "Reason: BEM-931 moved curator runtime to codex-local.yml on self-hosted Codex CLI."
          echo "No Gemini/OpenAI HTTP API call is performed by this workflow."
          echo "Use .github/workflows/codex-local.yml instead."
          exit 0
