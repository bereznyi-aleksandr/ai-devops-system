name: {
on:'
on:
  workflow_dispatch:
jobs:'
jobs:
  disabled-placeholder:
runs-on: ubuntu-latest'
name: {
on:
  workflow_dispatch:
jobs:
  disabled-placeholder:
{name: gci, on: {workflow_dispatch: {}}, jobs: {noop: {
