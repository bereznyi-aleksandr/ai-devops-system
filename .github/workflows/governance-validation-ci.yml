name: governance-validation-ci
on:'
Insert in
on:
  workflow_dispatch:
jobs:'
Insert in
jobs:
  disabled-placeholder:
runs-on: ubuntu-latest'
Insert in
name: governance-validation-ci
on:
  workflow_dispatch:
jobs:
  disabled-placeholder:
