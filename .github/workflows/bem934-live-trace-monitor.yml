name: BEM934 Live Trace Monitor Repaired Legacy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  guard:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Legacy workflow syntax guard
        run: |
          echo "BEM-937: legacy workflow syntax repaired."
          echo "This historical BEM-934 workflow is retained only as a valid manual guard."
          echo "Canonical proofs remain in governance/proofs/."
          echo "No release status is promoted by this legacy workflow."
