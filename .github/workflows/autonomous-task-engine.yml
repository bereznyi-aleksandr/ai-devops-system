name: Autonomous Task Engine

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Disabled by BEM-473 emergency circuit breaker'
        required: false
        default: 'mail_storm_stop'
        type: string

permissions:
  contents: read

jobs:
  disabled:
    name: Disabled by BEM-473
    runs-on: ubuntu-latest
    steps:
      - run: echo 'BEM-473: Autonomous Task Engine is temporarily disabled to stop failure storm.'
