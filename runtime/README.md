# Runtime State

This directory stores runtime verification data.

Runtime state includes:

- deployment state
- verified service URLs
- revision information
- baseline snapshots
- rollback checkpoints

The runtime layer represents the **real system state**.

No architecture decisions are stored here.

Only verified operational data.
